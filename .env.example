# 环境变量示例（不要填写真实值，提交到仓库仅作占位）
# 说明：Vite 前端使用以 `VITE_` 开头的变量；后端或本地工具可使用非 `VITE_` 变量名。

# —— 前端（Vite）环境变量 ——
# Supabase 项目地址（示例，请勿填写真实值）
VITE_SUPABASE_URL=
# Supabase 匿名 Key（示例，请勿填写真实值）
VITE_SUPABASE_ANON_KEY=

# —— 服务端/工具（可选）环境变量 ——
# 如果需要在脚本或云函数中使用，也可采用无 VITE_ 前缀的变量名
SUPABASE_URL=
SUPABASE_ANON_KEY=
OPENAI_API_KEY=

# 提示：实际 `.env` 文件已被 .gitignore 忽略，切勿提交真实值到仓库
