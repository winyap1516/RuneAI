# 仓库设置配置
repository:
  # 更新默认分支
  default_branch: main
  
  # 其他设置
  name: RuneAI
  description: 一个用于收藏网页跟AI小助理网页
  private: false
  
  # 启用功能
  has_issues: true
  has_projects: false
  has_wiki: false
  
  # 合并设置
  allow_squash_merge: true
  allow_merge_commit: true
  allow_rebase_merge: true
  
  # 分支保护（可选）
  # branches:
  #   - name: main
  #     protection:
  #       required_pull_request_reviews:
  #         required_approving_review_count: 1
  #       enforce_admins: true