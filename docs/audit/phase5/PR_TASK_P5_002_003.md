# PR: Implement Auth UI Logic (TASK-P5-002) & Connect Sync (TASK-P5-003)

## 📝 Description
Implemented `js/features/auth_ui.js` to handle login/signup form submissions using the new `supabase` client.
Connected Auth state changes to trigger data synchronization (`initSyncAfterLogin`).

## 🔗 Linked Tasks
- [x] TASK-P5-002: Implement Auth UI Logic
- [x] TASK-P5-003: Connect Auth to Sync Trigger

## 🛠️ Changes
- `js/features/auth_ui.js`:
  - Created `initAuthUI()` to listen for `onAuthStateChange`.
  - Implemented `handleLoginSuccess` to save user info locally and redirect.
  - Implemented `bindLoginForm` / `bindSignupForm` / `bindLogoutButton` to handle UI events.
  - Calls `initSyncAfterLogin` upon successful login.
- `js/main.js`:
  - Imported and called `initAuthUI()` during app initialization.

## ✅ Verification
- [x] Login form submission calls `supabase.auth.signInWithPassword`.
- [x] Signup form submission calls `supabase.auth.signUp`.
- [x] `onAuthStateChange` triggers `handleLoginSuccess`.
- [x] `handleLoginSuccess` calls `storageAdapter.saveUser` and `initSyncAfterLogin`.
- [x] Logout clears local storage and redirects to landing page.
